import React from "react";

export default function App() {

    return (
        <div>
            <h1>SSR REact App</h1>
            <button onClick={() => { alert("hii") }}>Click</button>
        </div>
    )
}